name: Test Audiobook Tool

on:
  push:
    branches:
      - main

env:
  UV_CACHE_DIR: /tmp/.uv-cache
  PLAYWRIGHT_BROWSERS_PATH: ${{ github.workspace }}/.cache/ms-playwright

jobs:
  test-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          cache-dependency-path: "uv.lock"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version-file: ".python-version"

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends ffmpeg

      - name: Cache Playwright Browsers
        uses: actions/cache@v4
        with:
          path: ${{ env.PLAYWRIGHT_BROWSERS_PATH }}
          key: ${{ runner.os }}-playwright-${{ hashFiles('uv.lock') }}
          restore-keys: |
            ${{ runner.os }}-playwright-

      - name: Install dependencies
        run: uv sync --frozen --extra dev

      - name: Install Playwright Chromium
        run: |
          uv run playwright install-deps chromium
          uv run playwright install chromium

      - name: Run Tool Check
        run: uv run audiobook-tool -h

      - name: Execute tests
        if: !startsWith(github.ref , 'refs/tags/')
        run: uv run pytest -k "not audible"

      - name: Execute tests with coverage (only new tags)
        if: startsWith(github.ref, 'refs/tags/')
        run: uv run pytest -k "not audible" --cov --cov-branch --cov-report=xml

      - name: Send code coverage (only new tags)
        if: startsWith(github.ref, 'refs/tags/')
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage.xml
          verbose: false
